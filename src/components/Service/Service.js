import React from 'react';

const Service = ({service}) => {
    console.log(service);
    return (
        <div>
            <h1>This is Service</h1>
        </div>
    );
};

export default Service;